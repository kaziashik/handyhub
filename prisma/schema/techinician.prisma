model Techinician {
  id String @id @default(uuid())

  name    String
  email   String  @unique
  address String?

  specialization  String
  licenseNumber   String @unique
  qualifications String
  experienceYears Int

  bio             String?
  consultationFee Decimal? @db.Decimal(10, 2)

  contactNumber String?

  verificationStatus TechinicianVerificationStatus @default(PENDING)
  rejectionReason    String?
  reviewedBy         String?
  reviewedAt         DateTime?

  resume          String?
  resumePublicId   String?
  additionalFiles Json? // Array of file URLs or file metadata // Stores: [{ url: "...", publicId: "..." }]


  isDeleted Boolean   @default(false)
  deletedAt DateTime?

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  userId String @unique
  user   user   @relation(fields: [userId], references: [id], onDelete: Cascade, onUpdate: Cascade)

   schedules Schedule[]
    appointments Apppointment[]

  @@index([email], name: "idx_techinician_email")
  @@map("techinician")
}
